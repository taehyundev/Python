#딕셔너리 사용법

a = {'name': 'taehyundev', 'age':20, 'birth':1107}

print(a['name'], a['age'], a['birth'])

#이렇게 value값을 key값을 배열 index처럼 사용해서 불러 올 수 있다.

#딕셔너리 만들때 주의할 사항

#key는 고유한 값이므로 중복되는 key를 사용하면 안됨

